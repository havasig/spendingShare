import 'package:flutter/material.dart';

class IncomeRowItem extends StatelessWidget {
  const IncomeRowItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Income row item');
  }
}
