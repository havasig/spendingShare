import 'dart:ui';

class Group {
  String name;
  String defaultCurrency;
  Color color;
  String logo;

  /*
  members
  admin
   */

  Group(this.name, this.defaultCurrency, this.color, this.logo);
}
